main {
    let char foo = getchar();
    let char bar = getchar();

    if foo == 'a' {
        for (let char i = foo; i < bar; i = i + 1 as char) {
            if i != 'b' {
                for (let char j = 0 as char; j < 5 as char; j = j + 1 as char) {
                    putchar(i + j);

                    if i + j == 'z' {
                        putchar('!');
                        return;
                    }
                }
                putchar('\n');
            }
        }
    } else {
        for (let int i = foo; i < bar; i = i + 1) {
            putchar(i);
        }
    }
    
}