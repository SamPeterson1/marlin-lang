// Valid nested scopes with complex variable usage

main {
    let int a = 1;
    
    {
        let int b = a + 2;
        
        {
            let int c = a + b;
            
            {
                let int d = a + b + c;
                let int e = d * 2;
            }
            
            let int f = a + b + c;
        }
        
        let int g = a + b;
    }
    
    let int h = a;
}
