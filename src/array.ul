fn main() -> int {
    let *int arr = alloc int[4];
    arr[0] = 1;
    arr[2] = 8;
    arr[1] = 7;
    arr[3] = -1;

    let int sum = 0;

    for (let int i = 0; i < 4; i = i + 1;) {
        sum = sum + arr[i];
    }

    sum
}